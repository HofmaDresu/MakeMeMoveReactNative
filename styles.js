import { PrimaryColor } from './colors';
import { StyleSheet, } from 'react-native';

export default StyleSheet.create({
  standardText: {
    color: PrimaryColor
  },
  indent: {
      marginLeft:20,
  },
  contentArea: {
      margin:20,
  },
})